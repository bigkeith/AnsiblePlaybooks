---
- name: Install NFS Client on Kubernetes Cluster
  hosts: k3s  # Use the group name from your inventory
  become: true
  tasks:
    - name: Update apt cache and install nfs-common
      apt:
        name: nfs-common
        state: present
        update_cache: yes